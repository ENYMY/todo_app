import React from "react";
import classes from "./Header.module.css";
const Header = (propsclear
    ) => {
  return (
    <div>
      <p>Todo application name</p>
      <p>Add Todo</p>
      <p>Favorites</p>
      <p>Login/ Logout</p>
    </div>
  );
};

export default Header;
